name: Daily Scraper Pipeline

# Complete automated workflow:
# 1. Scrape Instagram posts from 20 accounts using 3 parallel sessions (configurable)
# 2. Extract structured data using Gemini AI + OCR
# 3. Insert/update data in PostgreSQL database
# 4. Clean up expired opportunities and duplicates
# 5. Verify and report results

on:
  schedule:
    # Random schedule across the week (anti-detection)
    # Different time each day to avoid predictable patterns
    - cron: '0 6 * * 1'   # Monday 06:00 UTC (13:00 WIB)
    - cron: '0 13 * * 2'  # Tuesday 13:00 UTC (20:00 WIB)
    - cron: '0 21 * * 3'  # Wednesday 21:00 UTC (04:00 WIB next day)
    - cron: '0 9 * * 4'   # Thursday 09:00 UTC (16:00 WIB)
    - cron: '0 15 * * 5'  # Friday 15:00 UTC (22:00 WIB)
    - cron: '0 2 * * 6'   # Saturday 02:00 UTC (09:00 WIB)
    - cron: '0 11 * * 0'  # Sunday 11:00 UTC (18:00 WIB)
  
  workflow_dispatch:  # Allow manual trigger for testing
    inputs:
      skip_scraping:
        description: 'Skip scraping step (use existing data)'
        required: false
        type: boolean
        default: false
      accounts:
        description: 'Accounts to scrape (comma-separated, leave empty for all)'
        required: false
        type: string
        default: ''

env:
  PYTHON_VERSION: '3.11'
  NODE_VERSION: '20'

jobs:
  # Job 1: Scrape Instagram posts (Multi-Session Parallel)
  scrape:
    name: Scrape Instagram Posts (Multi-Session)
    runs-on: ubuntu-latest
    if: ${{ !inputs.skip_scraping }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Random startup delay (anti-detection)
        run: |
          # Random delay between 10-40 minutes to avoid predictable patterns
          DELAY=$((RANDOM % 1800 + 600))
          MINUTES=$((DELAY / 60))
          SECONDS=$((DELAY % 60))
          
          echo "⏰ Anti-detection: Random startup delay"
          echo "   Waiting: ${MINUTES}m ${SECONDS}s"
          echo "   This makes scraping time unpredictable"
          
          sleep $DELAY
          
          echo "✅ Delay complete - starting scraper"
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: scraper/package-lock.json
      
      - name: Install Node dependencies
        run: |
          cd scraper
          npm ci
      
      - name: Install Playwright browsers
        run: |
          cd scraper
          npx playwright install chromium
          npx playwright install-deps chromium
      
      - name: Create scraper config
        run: |
          # Use custom accounts if provided via manual input
          ACCOUNTS="${{ inputs.accounts }}"
          if [ -n "$ACCOUNTS" ]; then
            # Convert comma-separated to JSON array for manual input
            ACCOUNTS_JSON=$(echo "$ACCOUNTS" | jq -R 'split(",") | map(select(length > 0))')
            
            # Create config with manual accounts
            cat > config/scraper.config.json << EOF
          {
            "accounts": $ACCOUNTS_JSON,
            "scrollCount": 2,
            "deepScrapeMode": true,
            "downloadImages": true,
            "batchSize": 25,
            "delayBetweenRequests": 5
          }
          EOF
            
            echo "Created scraper config for manual accounts: $ACCOUNTS"
          else
            # Use existing config file (default behavior)
            echo "Using existing scraper config with all configured accounts"
          fi
          
          # Show current config
          echo "Current scraper configuration:"
          cat config/scraper.config.json
          
          # Extract and display account list
          ACCOUNT_LIST=$(jq -r '.accounts | join(", ")' config/scraper.config.json)
          ACCOUNT_COUNT=$(jq -r '.accounts | length' config/scraper.config.json)
          echo "Will scrape $ACCOUNT_COUNT accounts: $ACCOUNT_LIST"
      
      - name: Create session files from secrets (dynamic)
        run: |
          cd scraper
          
          # Auto-detect and create all available session secrets (scalable)
          SESSION_COUNT=0
          
          for i in {1..10}; do
            SECRET_NAME="INSTAGRAM_SESSION_$i"
            SECRET_VALUE="${!SECRET_NAME}"
            
            # Check if secret exists and is not empty
            if [ ! -z "$SECRET_VALUE" ] && [ "$SECRET_VALUE" != "null" ]; then
              echo "$SECRET_VALUE" > "session$i.json"
              FILE_SIZE=$(wc -c < "session$i.json")
              echo "✓ Session $i created ($FILE_SIZE bytes)"
              SESSION_COUNT=$((SESSION_COUNT + 1))
            fi
          done
          
          if [ $SESSION_COUNT -eq 0 ]; then
            echo "❌ ERROR: No Instagram sessions found!"
            echo "Please add INSTAGRAM_SESSION_1, INSTAGRAM_SESSION_2, etc. to GitHub Secrets"
            exit 1
          fi
          
          echo "✓ Created $SESSION_COUNT session files"
          echo "session_count=$SESSION_COUNT" >> $GITHUB_OUTPUT
        env:
          INSTAGRAM_SESSION_1: ${{ secrets.INSTAGRAM_SESSION_1 }}
          INSTAGRAM_SESSION_2: ${{ secrets.INSTAGRAM_SESSION_2 }}
          INSTAGRAM_SESSION_3: ${{ secrets.INSTAGRAM_SESSION_3 }}
          INSTAGRAM_SESSION_4: ${{ secrets.INSTAGRAM_SESSION_4 }}
          INSTAGRAM_SESSION_5: ${{ secrets.INSTAGRAM_SESSION_5 }}
          INSTAGRAM_SESSION_6: ${{ secrets.INSTAGRAM_SESSION_6 }}
          INSTAGRAM_SESSION_7: ${{ secrets.INSTAGRAM_SESSION_7 }}
          INSTAGRAM_SESSION_8: ${{ secrets.INSTAGRAM_SESSION_8 }}
          INSTAGRAM_SESSION_9: ${{ secrets.INSTAGRAM_SESSION_9 }}
          INSTAGRAM_SESSION_10: ${{ secrets.INSTAGRAM_SESSION_10 }}
          INSTAGRAM_PASSWORD_1: ${{ secrets.INSTAGRAM_PASSWORD_1 }}
          INSTAGRAM_PASSWORD_2: ${{ secrets.INSTAGRAM_PASSWORD_2 }}
          INSTAGRAM_PASSWORD_3: ${{ secrets.INSTAGRAM_PASSWORD_3 }}
          INSTAGRAM_PASSWORD_4: ${{ secrets.INSTAGRAM_PASSWORD_4 }}
          INSTAGRAM_PASSWORD_5: ${{ secrets.INSTAGRAM_PASSWORD_5 }}
          INSTAGRAM_PASSWORD_6: ${{ secrets.INSTAGRAM_PASSWORD_6 }}
          INSTAGRAM_PASSWORD_7: ${{ secrets.INSTAGRAM_PASSWORD_7 }}
          INSTAGRAM_PASSWORD_8: ${{ secrets.INSTAGRAM_PASSWORD_8 }}
          INSTAGRAM_PASSWORD_9: ${{ secrets.INSTAGRAM_PASSWORD_9 }}
          INSTAGRAM_PASSWORD_10: ${{ secrets.INSTAGRAM_PASSWORD_10 }}
      
      - name: Download previous scraper checkpoint
        id: download-scraper-checkpoint
        uses: actions/download-artifact@v4
        with:
          name: scraper-checkpoint-${{ github.repository_owner }}
          path: scraper/
        continue-on-error: true
      
      - name: Check for scraper checkpoint
        id: check-scraper-checkpoint
        run: |
          echo "🔍 Checking for previous scraper checkpoint..."
          
          if [ -f "scraper/scraper_checkpoint.json" ]; then
            echo "checkpoint_found=true" >> $GITHUB_OUTPUT
            
            # Parse checkpoint info
            COMPLETED=$(jq -r '.completed_accounts | length' scraper/scraper_checkpoint.json)
            REMAINING=$(jq -r '.remaining_accounts | length' scraper/scraper_checkpoint.json)
            TOTAL=$(jq -r '.total_accounts' scraper/scraper_checkpoint.json)
            PROGRESS=$(jq -r '.progress_percentage' scraper/scraper_checkpoint.json)
            CREATED_AT=$(jq -r '.created_at' scraper/scraper_checkpoint.json)
            
            echo "📍 Checkpoint found!"
            echo "  - Completed: $COMPLETED accounts"
            echo "  - Remaining: $REMAINING accounts"
            echo "  - Progress: $PROGRESS%"
            echo "  - Created at: $CREATED_AT"
            echo "  - Will resume from remaining accounts"
            
            # Add to job summary
            echo "## 📍 Resuming Scraper from Checkpoint" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- **Completed**: $COMPLETED accounts" >> $GITHUB_STEP_SUMMARY
            echo "- **Remaining**: $REMAINING accounts" >> $GITHUB_STEP_SUMMARY
            echo "- **Progress**: $PROGRESS%" >> $GITHUB_STEP_SUMMARY
            echo "- **Created**: $CREATED_AT" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "checkpoint_found=false" >> $GITHUB_OUTPUT
            echo "🆕 No checkpoint found: Starting from beginning"
            
            # Add to job summary
            echo "## 🆕 Starting Fresh Scrape" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "No previous checkpoint found. Processing all accounts from the beginning." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
      
      - name: Run Instagram scraper (Multi-Session Parallel)
        id: scrape
        run: |
          cd scraper
          node scraper-parallel.js
        timeout-minutes: 20
      
      - name: Check scraping results
        run: |
          if [ -f "scraper/instagram_data.json" ]; then
            # Count total posts across all accounts
            POST_COUNT=$(jq '[.[] | length] | add' scraper/instagram_data.json)
            echo "✓ Scraped $POST_COUNT posts"
            echo "post_count=$POST_COUNT" >> $GITHUB_OUTPUT
          else
            echo "✗ No data file found"
            exit 1
          fi
      
      - name: Clear scraper checkpoint on success
        if: success()
        run: |
          echo "🧹 Clearing scraper checkpoint (scraping completed successfully)"
          
          # Note: Checkpoint file is already cleared by scraper-parallel.js
          # This step is for logging and documentation purposes
          
          if [ ! -f "scraper/scraper_checkpoint.json" ]; then
            echo "✓ Checkpoint cleared successfully"
            echo "✓ All accounts processed - no resume needed"
          else
            echo "⚠️ Checkpoint file still exists (partial completion)"
            REMAINING=$(jq -r '.remaining_accounts | length' scraper/scraper_checkpoint.json)
            echo "⚠️ $REMAINING accounts still remaining"
          fi
      
      - name: Upload scraped data
        uses: actions/upload-artifact@v4
        with:
          name: scraped-data-${{ github.run_number }}
          path: |
            scraper/instagram_data.json
            scraper/instagram_images/
          retention-days: 7
      
      - name: Upload debug screenshots (if any)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots-${{ github.run_number }}
          path: scraper/debug_screenshots/
          retention-days: 7
          if-no-files-found: ignore
      
      - name: Upload scraper checkpoint on failure
        if: failure() || cancelled()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-checkpoint-${{ github.repository_owner }}
          path: scraper/scraper_checkpoint.json
          retention-days: 7
          if-no-files-found: warn
      
      - name: Log scraper checkpoint upload
        if: failure() || cancelled()
        run: |
          if [ -f "scraper/scraper_checkpoint.json" ]; then
            COMPLETED=$(jq -r '.completed_accounts | length' scraper/scraper_checkpoint.json)
            REMAINING=$(jq -r '.remaining_accounts | length' scraper/scraper_checkpoint.json)
            TOTAL=$(jq -r '.total_accounts' scraper/scraper_checkpoint.json)
            PROGRESS=$(jq -r '.progress_percentage' scraper/scraper_checkpoint.json)
            
            echo "💾 Scraper checkpoint uploaded for resume"
            echo "  - Completed: $COMPLETED accounts"
            echo "  - Remaining: $REMAINING accounts"
            echo "  - Progress: $PROGRESS%"
            echo "  - To resume: Re-run this workflow"
            
            # Add to job summary
            echo "## 💾 Scraper Checkpoint Saved" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Scraping failed/timed out, but progress has been saved:" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- **Completed**: $COMPLETED accounts" >> $GITHUB_STEP_SUMMARY
            echo "- **Remaining**: $REMAINING accounts" >> $GITHUB_STEP_SUMMARY
            echo "- **Progress**: $PROGRESS%" >> $GITHUB_STEP_SUMMARY
            echo "- **Next step**: Re-run this workflow to resume" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No checkpoint file found to upload"
          fi
      
      - name: Scraping summary
        if: always()
        run: |
          echo "## 📸 Scraping Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f "scraper/instagram_data.json" ]; then
            # Count total posts across all accounts
            POST_COUNT=$(jq '[.[] | length] | add' scraper/instagram_data.json)
            
            # Generate account summary
            ACCOUNTS=$(jq -r 'to_entries | .[] | "- **@\(.key)**: \(.value | length) posts"' scraper/instagram_data.json)
            
            echo "**Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
            echo "**Total Posts**: $POST_COUNT" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Posts by Account" >> $GITHUB_STEP_SUMMARY
            echo "$ACCOUNTS" >> $GITHUB_STEP_SUMMARY
          else
            echo "**Status**: ❌ Failed" >> $GITHUB_STEP_SUMMARY
          fi

  # Job 2: Extract structured data using AI
  extract:
    name: Extract Structured Data
    runs-on: ubuntu-latest
    needs: scrape
    if: ${{ !cancelled() && (needs.scrape.result == 'success' || inputs.skip_scraping) }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Install Tesseract OCR
        run: |
          sudo apt-get update
          sudo apt-get install -y tesseract-ocr tesseract-ocr-ind
          tesseract --version
      
      - name: Download scraped data
        if: ${{ !inputs.skip_scraping }}
        uses: actions/download-artifact@v4
        with:
          name: scraped-data-${{ github.run_number }}
          path: scraper/
      
      - name: Download previous checkpoint
        id: download-checkpoint
        uses: actions/download-artifact@v4
        with:
          name: extraction-checkpoint-${{ github.repository_owner }}
          path: data/processed/
        continue-on-error: true
      
      - name: Check for checkpoint
        id: check-checkpoint
        run: |
          echo "🔍 Checking for previous checkpoint..."
          
          if [ -f "data/processed/checkpoint_state.json" ]; then
            echo "checkpoint_found=true" >> $GITHUB_OUTPUT
            
            # Parse checkpoint info
            LAST_INDEX=$(jq -r '.last_completed_index' data/processed/checkpoint_state.json)
            TOTAL=$(jq -r '.total_accounts' data/processed/checkpoint_state.json)
            LAST_ACCOUNT=$(jq -r '.last_completed_account' data/processed/checkpoint_state.json)
            RESULTS_COUNT=$(jq -r '.results_count' data/processed/checkpoint_state.json)
            CREATED_AT=$(jq -r '.created_at' data/processed/checkpoint_state.json)
            
            echo "📍 Checkpoint found!"
            echo "  - Last completed: @$LAST_ACCOUNT"
            echo "  - Progress: $((LAST_INDEX + 1))/$TOTAL accounts"
            echo "  - Results so far: $RESULTS_COUNT posts"
            echo "  - Created at: $CREATED_AT"
            echo "  - Will resume from account $((LAST_INDEX + 2))/$TOTAL"
            
            # Add to job summary
            echo "## 📍 Resuming from Checkpoint" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- **Last completed**: @$LAST_ACCOUNT" >> $GITHUB_STEP_SUMMARY
            echo "- **Progress**: $((LAST_INDEX + 1))/$TOTAL accounts" >> $GITHUB_STEP_SUMMARY
            echo "- **Results so far**: $RESULTS_COUNT posts" >> $GITHUB_STEP_SUMMARY
            echo "- **Resuming from**: Account $((LAST_INDEX + 2))/$TOTAL" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "checkpoint_found=false" >> $GITHUB_OUTPUT
            echo "🆕 No checkpoint found: Starting from beginning"
            
            # Add to job summary
            echo "## 🆕 Starting Fresh" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "No previous checkpoint found. Processing all accounts from the beginning." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
      
      - name: Create .env file
        run: |
          cat > config/.env << EOF
          # Gemini API Keys (5 keys for proactive rotation)
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}
          
          # OpenRouter API Keys (5 keys for rotation)
          OPENROUTER_API_KEY=${{ secrets.OPENROUTER_API_KEY }}
          
          # Database
          DATABASE_URL=${{ secrets.DATABASE_URL }}
          
          # Extractor Settings (optimized configuration)
          BATCH_SIZE=25
          DELAY_BETWEEN_REQUESTS=5
          GEMINI_MODEL=gemini-3.1-flash-lite-preview
          
          # AI Service Configuration (NEW - Optimized for GitHub Actions)
          PRIMARY_SERVICE=openrouter
          MAX_RETRY_ATTEMPTS=3
          MAX_BACKOFF_SECONDS=10
          EOF
          
          echo "✓ Configuration created"
      
      - name: Run AI extraction
        id: extract
        run: |
          python src/extraction/main.py scraper/instagram_data.json
        timeout-minutes: 30
      
      - name: Upload checkpoint on failure
        if: failure() || cancelled()
        uses: actions/upload-artifact@v4
        with:
          name: extraction-checkpoint-${{ github.repository_owner }}
          path: |
            data/processed/checkpoint_state.json
            data/processed/checkpoint_results.json
          retention-days: 7
          if-no-files-found: warn
      
      - name: Log checkpoint upload
        if: failure() || cancelled()
        run: |
          if [ -f "data/processed/checkpoint_state.json" ]; then
            LAST_INDEX=$(jq -r '.last_completed_index' data/processed/checkpoint_state.json)
            TOTAL=$(jq -r '.total_accounts' data/processed/checkpoint_state.json)
            RESULTS_COUNT=$(jq -r '.results_count' data/processed/checkpoint_state.json)
            
            echo "💾 Checkpoint uploaded for resume"
            echo "  - Progress saved: $((LAST_INDEX + 1))/$TOTAL accounts"
            echo "  - Results saved: $RESULTS_COUNT posts"
            echo "  - To resume: Re-run this workflow"
            
            # Add to job summary
            echo "## 💾 Checkpoint Saved" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Workflow failed/timed out, but progress has been saved:" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- **Progress saved**: $((LAST_INDEX + 1))/$TOTAL accounts" >> $GITHUB_STEP_SUMMARY
            echo "- **Results saved**: $RESULTS_COUNT posts" >> $GITHUB_STEP_SUMMARY
            echo "- **Next step**: Re-run this workflow to resume" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No checkpoint file found to upload"
          fi
      
      - name: Check extraction results
        run: |
          LATEST_FILE=$(ls -t data/processed/extracted_data_*.json 2>/dev/null | head -1)
          if [ -n "$LATEST_FILE" ]; then
            EXTRACTED_COUNT=$(jq 'length' "$LATEST_FILE")
            echo "✓ Extracted $EXTRACTED_COUNT posts"
            echo "extracted_count=$EXTRACTED_COUNT" >> $GITHUB_OUTPUT
            echo "extracted_file=$LATEST_FILE" >> $GITHUB_OUTPUT
          else
            echo "✗ No extracted data file found"
            exit 1
          fi
      
      - name: Upload extracted data
        uses: actions/upload-artifact@v4
        with:
          name: extracted-data-${{ github.run_number }}
          path: data/processed/*.json
          retention-days: 7
      
      - name: Extraction summary
        if: always()
        run: |
          echo "## 🧠 Extraction Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Check if extraction step succeeded
          if [ "${{ steps.extract.outcome }}" == "success" ]; then
            LATEST_FILE=$(ls -t data/processed/extracted_data_*.json 2>/dev/null | head -1)
            if [ -n "$LATEST_FILE" ]; then
              EXTRACTED_COUNT=$(jq 'length' "$LATEST_FILE")
              
              # Calculate success rate
              if [ -f "scraper/instagram_data.json" ]; then
                SCRAPED_COUNT=$(jq 'length' scraper/instagram_data.json)
                SUCCESS_RATE=$(echo "scale=1; $EXTRACTED_COUNT * 100 / $SCRAPED_COUNT" | bc)
                echo "**Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
                echo "**Success Rate**: ${SUCCESS_RATE}%" >> $GITHUB_STEP_SUMMARY
                echo "**Extracted**: $EXTRACTED_COUNT / $SCRAPED_COUNT posts" >> $GITHUB_STEP_SUMMARY
              else
                echo "**Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
                echo "**Extracted**: $EXTRACTED_COUNT posts" >> $GITHUB_STEP_SUMMARY
              fi
              
              # Show metrics if available
              METRICS_FILE=$(ls -t data/processed/extraction_metrics_*.json 2>/dev/null | head -1)
              if [ -n "$METRICS_FILE" ]; then
                echo "" >> $GITHUB_STEP_SUMMARY
                echo "### Metrics" >> $GITHUB_STEP_SUMMARY
                jq -r '"- **API Requests**: \(.total_api_requests // 0)\n- **Processing Time**: \(.total_duration_seconds // 0)s\n- **Avg Response Time**: \(.avg_response_time_seconds // 0)s"' "$METRICS_FILE" >> $GITHUB_STEP_SUMMARY
              fi
            else
              echo "**Status**: ⚠️ Completed but no output file" >> $GITHUB_STEP_SUMMARY
            fi
          elif [ "${{ steps.extract.outcome }}" == "failure" ]; then
            echo "**Status**: ❌ Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Error Details" >> $GITHUB_STEP_SUMMARY
            echo "The extraction process failed. Common causes:" >> $GITHUB_STEP_SUMMARY
            echo "- **Gemini API errors**: Check if API keys are valid and have quota" >> $GITHUB_STEP_SUMMARY
            echo "- **Network issues**: Temporary connectivity problems" >> $GITHUB_STEP_SUMMARY
            echo "- **Data format issues**: Invalid input data structure" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Action Required**: Check workflow logs above for specific error messages" >> $GITHUB_STEP_SUMMARY
            
            # Check if checkpoint was saved
            if [ -f "data/processed/checkpoint_state.json" ]; then
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "### 💾 Checkpoint Available" >> $GITHUB_STEP_SUMMARY
              echo "Progress was saved. You can re-run this workflow to resume." >> $GITHUB_STEP_SUMMARY
            fi
          elif [ "${{ steps.extract.outcome }}" == "cancelled" ]; then
            echo "**Status**: ⏸️ Cancelled (Timeout)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Timeout Details" >> $GITHUB_STEP_SUMMARY
            echo "The extraction exceeded the 30-minute timeout limit." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Possible causes**:" >> $GITHUB_STEP_SUMMARY
            echo "- Gemini API experiencing high demand (503 errors)" >> $GITHUB_STEP_SUMMARY
            echo "- Too many accounts to process in 30 minutes" >> $GITHUB_STEP_SUMMARY
            echo "- Network latency issues" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Check if checkpoint was saved
            if [ -f "data/processed/checkpoint_state.json" ]; then
              LAST_INDEX=$(jq -r '.last_completed_index' data/processed/checkpoint_state.json)
              TOTAL=$(jq -r '.total_accounts' data/processed/checkpoint_state.json)
              RESULTS_COUNT=$(jq -r '.results_count' data/processed/checkpoint_state.json)
              
              echo "### 💾 Checkpoint Saved" >> $GITHUB_STEP_SUMMARY
              echo "- **Progress**: $((LAST_INDEX + 1))/$TOTAL accounts completed" >> $GITHUB_STEP_SUMMARY
              echo "- **Results**: $RESULTS_COUNT posts extracted" >> $GITHUB_STEP_SUMMARY
              echo "- **Action**: Re-run this workflow to resume from checkpoint" >> $GITHUB_STEP_SUMMARY
            fi
          fi

  # Job 3: Upload to R2 (NEW - BEFORE database)
  upload-to-r2:
    name: Upload Images to R2
    runs-on: ubuntu-latest
    needs: extract
    if: ${{ !cancelled() && needs.extract.result == 'success' }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Download extracted data
        uses: actions/download-artifact@v4
        with:
          name: extracted-data-${{ github.run_number }}
          path: data/processed/
      
      - name: Download scraped images
        uses: actions/download-artifact@v4
        with:
          name: scraped-data-${{ github.run_number }}
          path: scraper/
      
      - name: Create .env file
        run: |
          cat > config/.env << EOF
          R2_ACCOUNT_ID=${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID=${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY=${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET_NAME=${{ secrets.R2_BUCKET_NAME }}
          R2_PUBLIC_URL=${{ secrets.R2_PUBLIC_URL }}
          EOF
          
          echo "✓ R2 configuration created"
      
      - name: Upload images to R2 and modify JSON
        id: upload
        run: |
          # Find latest extracted data file
          LATEST_FILE=$(ls -t data/processed/extracted_data_*.json 2>/dev/null | grep -v '_r2.json' | head -1)
          
          if [ -z "$LATEST_FILE" ]; then
            echo "❌ No extracted data file found"
            exit 1
          fi
          
          echo "📂 Input file: $LATEST_FILE"
          
          # Run R2 upload script
          python scripts/upload_to_r2_before_db.py "$LATEST_FILE"
          
          # Check output
          OUTPUT_FILE="${LATEST_FILE%.json}_r2.json"
          
          if [ ! -f "$OUTPUT_FILE" ]; then
            echo "❌ Output file not created: $OUTPUT_FILE"
            exit 1
          fi
          
          echo "✅ Modified JSON created: $OUTPUT_FILE"
          echo "output_file=$OUTPUT_FILE" >> $GITHUB_OUTPUT
        timeout-minutes: 15
      
      - name: Upload modified data
        uses: actions/upload-artifact@v4
        with:
          name: extracted-data-r2-${{ github.run_number }}
          path: data/processed/*_r2.json
          retention-days: 7
      
      - name: R2 upload summary
        if: always()
        run: |
          echo "## 📤 R2 Upload Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.upload.outcome }}" == "success" ]; then
            echo "**Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Images uploaded to R2 and JSON modified with R2 URLs." >> $GITHUB_STEP_SUMMARY
            echo "Database will be populated with R2 URLs directly." >> $GITHUB_STEP_SUMMARY
          else
            echo "**Status**: ❌ Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "R2 upload failed. Database insertion will be skipped." >> $GITHUB_STEP_SUMMARY
          fi

  # Job 4: Insert/update database (MODIFIED - use R2 JSON)
  database:
    name: Update Database
    runs-on: ubuntu-latest
    needs: upload-to-r2
    if: ${{ !cancelled() && needs.upload-to-r2.result == 'success' }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Download modified data (R2 URLs)
        uses: actions/download-artifact@v4
        with:
          name: extracted-data-r2-${{ github.run_number }}
          path: data/processed/
      
      - name: Create .env file
        run: |
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > config/.env
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> config/.env
      
      - name: Run database insertion
        id: insert
        uses: nick-invision/retry@v3
        with:
          timeout_minutes: 20
          max_attempts: 2
          retry_wait_seconds: 30
          command: |
            # Verify file exists in correct location
            echo "🔍 Checking for R2-modified data files..."
            ls -la data/processed/ || echo "data/processed/ directory not found!"
            
            # Find the latest R2-modified data file
            LATEST_FILE=$(ls -t data/processed/extracted_data_*_r2.json 2>/dev/null | head -1)
            
            if [ -z "$LATEST_FILE" ]; then
              echo "❌ ERROR: No R2-modified data file found in data/processed/"
              echo "Expected: data/processed/extracted_data_*_r2.json"
              echo ""
              echo "Available files in data/processed/:"
              find data/processed -type f 2>/dev/null || echo "No files found"
              echo ""
              echo "Searching entire workspace:"
              find . -name "extracted_data_*.json" -type f 2>/dev/null || echo "No extracted_data files found anywhere"
              exit 1
            fi
            
            echo "✓ Found extracted data file: $LATEST_FILE"
            echo "📊 File size: $(wc -c < "$LATEST_FILE") bytes"
            
            RECORD_COUNT=$(jq 'length' "$LATEST_FILE" 2>/dev/null || echo "unknown")
            echo "📊 Record count: $RECORD_COUNT"
            echo ""
            echo "⏱️  Starting database insertion (timeout: 20 minutes)..."
            echo "🔄 Retry enabled: 2 attempts with 30s wait between retries"
            echo ""
            
            # Run insertion
            python src/database/main.py "$LATEST_FILE"
            
            # Check exit code
            EXIT_CODE=$?
            if [ $EXIT_CODE -ne 0 ]; then
              echo ""
              echo "❌ ERROR: Database insertion failed with exit code $EXIT_CODE"
              exit $EXIT_CODE
            fi
            
            echo ""
            echo "✅ Database insertion completed successfully"
      
      - name: Database summary
        if: always()
        run: |
          echo "## 🗄️ Database Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.insert.outcome }}" == "success" ]; then
            echo "**Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # Try to extract detailed stats from logs (Phase 1 Enhancement)
            # Look for the enhanced logging format we added
            echo "### Insertion Details" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Check workflow logs for detailed breakdown:" >> $GITHUB_STEP_SUMMARY
            echo "- ✅ Newly Inserted records" >> $GITHUB_STEP_SUMMARY
            echo "- 🔄 Updated Existing records" >> $GITHUB_STEP_SUMMARY
            echo "- ⏭️  Skipped records (expired/no dates)" >> $GITHUB_STEP_SUMMARY
            echo "- ❌ Database Errors" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "💡 **Note**: If all records show as 'Updated', this is normal - duplicate posts from previous runs are updated, not inserted again." >> $GITHUB_STEP_SUMMARY
          else
            echo "**Status**: ❌ Failed" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Check workflow logs for error details." >> $GITHUB_STEP_SUMMARY
          fi

  # Job 5: Cleanup and verification
  cleanup:
    name: Cleanup & Verify
    runs-on: ubuntu-latest
    needs: database
    if: always()  # Run even if previous jobs failed
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Create .env file
        run: |
          echo "DATABASE_URL=${{ secrets.DATABASE_URL }}" > config/.env
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> config/.env
      
      - name: 1️⃣ Mark expired opportunities
        id: cleanup_expired
        run: |
          echo "=================================================="
          echo "🕐 STEP 1: Marking Expired Opportunities"
          echo "=================================================="
          echo "⏱️  Start time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo ""
          
          START_TIME=$(date +%s)
          python src/database/cleanup_expired.py 2>&1 | tee cleanup_expired.log
          EXIT_CODE=${PIPESTATUS[0]}
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          echo ""
          echo "⏱️  End time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "⏱️  Duration: ${DURATION}s"
          echo "📊 Exit code: $EXIT_CODE"
          
          # Extract metrics from log
          EXPIRED_COUNT=$(grep -oP 'marked as expired: \K\d+' cleanup_expired.log || echo "0")
          echo "expired_count=$EXPIRED_COUNT" >> $GITHUB_OUTPUT
          echo "duration=$DURATION" >> $GITHUB_OUTPUT
          
          exit $EXIT_CODE
        continue-on-error: true
        timeout-minutes: 5
      
      - name: 2️⃣ Cleanup URL duplicates
        id: cleanup_duplicates
        run: |
          echo "=================================================="
          echo "🔗 STEP 2: Cleaning Up URL Duplicates"
          echo "=================================================="
          echo "⏱️  Start time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo ""
          
          START_TIME=$(date +%s)
          python src/database/cleanup_duplicates.py 2>&1 | tee cleanup_duplicates.log
          EXIT_CODE=${PIPESTATUS[0]}
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          echo ""
          echo "⏱️  End time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "⏱️  Duration: ${DURATION}s"
          echo "📊 Exit code: $EXIT_CODE"
          
          # Extract metrics from log
          DUPLICATE_GROUPS=$(grep -oP 'Total duplicate groups: \K\d+' cleanup_duplicates.log || echo "0")
          RECORDS_DELETED=$(grep -oP 'Total records deleted: \K\d+' cleanup_duplicates.log || echo "0")
          SUCCESSFUL_MERGES=$(grep -oP 'Successful merges: \K\d+' cleanup_duplicates.log || echo "0")
          FAILED_MERGES=$(grep -oP 'Failed merges: \K\d+' cleanup_duplicates.log || echo "0")
          
          echo "duplicate_groups=$DUPLICATE_GROUPS" >> $GITHUB_OUTPUT
          echo "records_deleted=$RECORDS_DELETED" >> $GITHUB_OUTPUT
          echo "successful_merges=$SUCCESSFUL_MERGES" >> $GITHUB_OUTPUT
          echo "failed_merges=$FAILED_MERGES" >> $GITHUB_OUTPUT
          echo "duration=$DURATION" >> $GITHUB_OUTPUT
          
          exit $EXIT_CODE
        continue-on-error: true
        timeout-minutes: 10
      
      - name: 3️⃣ Verify database integrity
        id: verify
        run: |
          echo "=================================================="
          echo "✅ STEP 3: Verifying Database Integrity"
          echo "=================================================="
          echo "⏱️  Start time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo ""
          
          START_TIME=$(date +%s)
          python scripts/verify.py 2>&1 | tee verify.log
          EXIT_CODE=${PIPESTATUS[0]}
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          echo ""
          echo "⏱️  End time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "⏱️  Duration: ${DURATION}s"
          echo "📊 Exit code: $EXIT_CODE"
          
          # Extract metrics from log
          TOTAL_OPPORTUNITIES=$(grep -oP 'Total opportunities: \K\d+' verify.log || echo "0")
          ACTIVE_COUNT=$(grep -oP 'Active: \K\d+' verify.log || echo "0")
          EXPIRED_COUNT=$(grep -oP 'Expired: \K\d+' verify.log || echo "0")
          
          echo "total_opportunities=$TOTAL_OPPORTUNITIES" >> $GITHUB_OUTPUT
          echo "active_count=$ACTIVE_COUNT" >> $GITHUB_OUTPUT
          echo "expired_count=$EXPIRED_COUNT" >> $GITHUB_OUTPUT
          echo "duration=$DURATION" >> $GITHUB_OUTPUT
          
          exit $EXIT_CODE
        continue-on-error: true
        timeout-minutes: 5
      
      - name: 📊 Cleanup summary
        if: always()
        run: |
          echo "## 🧹 Cleanup & Verification Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Step 1: Mark Expired
          echo "### 1️⃣ Mark Expired Opportunities" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ steps.cleanup_expired.outcome }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.cleanup_expired.outcome }}" == "success" ]; then
            echo "- **Expired Count**: ${{ steps.cleanup_expired.outputs.expired_count }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Duration**: ${{ steps.cleanup_expired.outputs.duration }}s" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Step 2: Cleanup Duplicates
          echo "### 2️⃣ Cleanup URL Duplicates" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ steps.cleanup_duplicates.outcome }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.cleanup_duplicates.outcome }}" == "success" ]; then
            echo "- **Duplicate Groups Found**: ${{ steps.cleanup_duplicates.outputs.duplicate_groups }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Records Deleted**: ${{ steps.cleanup_duplicates.outputs.records_deleted }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Successful Merges**: ${{ steps.cleanup_duplicates.outputs.successful_merges }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Failed Merges**: ${{ steps.cleanup_duplicates.outputs.failed_merges }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Duration**: ${{ steps.cleanup_duplicates.outputs.duration }}s" >> $GITHUB_STEP_SUMMARY
            
            # Add interpretation
            if [ "${{ steps.cleanup_duplicates.outputs.duplicate_groups }}" == "0" ]; then
              echo "- ✅ **No duplicates found** - URL duplicate detection is working!" >> $GITHUB_STEP_SUMMARY
            else
              echo "- ⚠️ **Duplicates detected** - Check if prevention is working correctly" >> $GITHUB_STEP_SUMMARY
            fi
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Step 3: Verification
          echo "### 3️⃣ Database Verification" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ steps.verify.outcome }}" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.verify.outcome }}" == "success" ]; then
            echo "- **Total Opportunities**: ${{ steps.verify.outputs.total_opportunities }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Active**: ${{ steps.verify.outputs.active_count }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Expired**: ${{ steps.verify.outputs.expired_count }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Duration**: ${{ steps.verify.outputs.duration }}s" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Overall cleanup status
          echo "### 📈 Overall Cleanup Status" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.cleanup_expired.outcome }}" == "success" ] && [ "${{ steps.cleanup_duplicates.outcome }}" == "success" ] && [ "${{ steps.verify.outcome }}" == "success" ]; then
            echo "✅ **All cleanup tasks completed successfully**" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ **Some cleanup tasks failed** - Check logs for details" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Performance summary
          TOTAL_DURATION=$((
            ${{ steps.cleanup_expired.outputs.duration || 0 }} + 
            ${{ steps.cleanup_duplicates.outputs.duration || 0 }} + 
            ${{ steps.verify.outputs.duration || 0 }}
          ))
          echo "**Total Cleanup Time**: ${TOTAL_DURATION}s" >> $GITHUB_STEP_SUMMARY

  # Job 5: Final report
  report:
    name: Pipeline Report
    runs-on: ubuntu-latest
    needs: [scrape, extract, database, cleanup]
    if: always()
    
    steps:
      - name: Generate pipeline report
        run: |
          echo "# 📊 Daily Pipeline Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Run Number**: ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Time**: $(date -u '+%Y-%m-%d %H:%M:%S UTC') (13:00 WIB)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Job Status" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Scraping | ${{ needs.scrape.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Extraction | ${{ needs.extract.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Database | ${{ needs.database.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Cleanup | ${{ needs.cleanup.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Overall status
          if [ "${{ needs.scrape.result }}" == "success" ] && [ "${{ needs.extract.result }}" == "success" ] && [ "${{ needs.database.result }}" == "success" ]; then
            echo "## ✅ Pipeline Status: SUCCESS" >> $GITHUB_STEP_SUMMARY
          else
            echo "## ⚠️ Pipeline Status: PARTIAL SUCCESS" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Next run: Tomorrow at 13:00 WIB (06:00 UTC)*" >> $GITHUB_STEP_SUMMARY
      
      - name: Send notification (optional)
        if: failure()
        run: |
          echo "Pipeline failed. Configure notifications here if needed."
          # Add Slack, Discord, or email notifications here
